// Testing out a handler for multiple usersin Arduino, provided we ever have multiple users sending (other than Google Phone)
// PLACEHOLDER PSEUDOCODE WRITTEN 10/3/2019
#define SENDERS 8 //this is how many possible phone numbers can be communicating with the device
#define ELEMENTS(x)    (sizeof(x) / sizeof(x[0])) // number of elements in an array
#define NUMBERS 12
int sender_count = 1  // checkTexts() should 

String numbers[SENDERS]; // countains our phone numbers
if(sender_count > 1){
buffer_send();
}
void buffer_send(array[Senders]){
int i = sender_count;
for (i, i >= 0, i--){
// read message from that number if that number has sent something
// return message to that number
}

}
