//I2C Tipping Bucket
#include <Wire.h>
#define  TIP_1 3
#define  TIP_2 4
// the setup routine runs once when you press reset:
void setup() {
 Wire.begin();
}
 
// the loop routine runs over and over again forever:
void loop() {
//boolean tip = touch();
// rain(tip)
//Wire.beginTransmission(Address x);   transmit to device at our address 
//Wire.write(byte(0x00));   Instruction byte
// delay(time);
}

void touch(void){

}

void rain(boolean t){

}
